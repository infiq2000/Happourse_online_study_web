$(".chapter").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=chapter1]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Chapter" : "+ Chapter";
        });
    });
 
});

$("[class=ct1]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c1]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 1" : "+ Content 1";
        });
    });
 
});

$("[class=ct2]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c2]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 2" : "+ Content 2";
        });
    });
 
});

$("[class=ct3]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c3]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 3" : "+ Content 3";
        });
    });
 
});

$("[class=ct4]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c4]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 4" : "+ Content 4";
        });
    });
 
});

$("[class=ct5]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c5]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 5" : "+ Content 5";
        });
    });
 
});

$("[class=ct6]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c6]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 6" : "+ Content 6";
        });
    });
 
});

$("[class=ct7]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c7]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 7" : "+ Content 7";
        });
    });
 
});

$("[class=ct8]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c8]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 8" : "+ Content 8";
        });
    });
 
});

$("[class=ct9]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c9]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 9" : "+ Content 9";
        });
    });
 
});

$("[class=ct10]").click(function () {
 
    $chapter = $(this);
    //getting the next element
    $chapter1 = $("[id=c10]");
    //open up the content needed - toggle the slide- if visible, slide up, if not slidedown.
    $chapter1.slideToggle(500, function () {
        //execute this after slideToggle is done
        //change text of header based on visibility of content div
        $chapter.text(function () {
            //change text based on condition
            return $chapter1.is(":visible") ? "- Content 10" : "+ Content 10";
        });
    });
 
});